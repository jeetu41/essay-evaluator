services:
  - type: web
    name: essay-evaluator
    env: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GEMINI_API_KEY
        fromService:
          type: secret
          name: gemini-api-key
      - key: PORT
        value: 8080
    plan: free
